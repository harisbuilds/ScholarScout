OPENAI_KEY=your-openai-api-key
TAVILY_API_KEY=your-tavily-api-key    # optional — only needed for web search

DB_HOST=localhost
DB_PORT=5432
DB_NAME=scholarscout
DB_USER=postgres
DB_PASSWORD=your-db-password

MILVUS_URI=http://localhost:19530     # default; change if Milvus runs elsewhere
